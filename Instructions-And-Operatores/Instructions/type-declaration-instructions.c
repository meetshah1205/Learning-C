#include <stdio.h>

int main()
{
    int a = 4;    // Type declared - int
    float b = 3;  // Type declared - float
    char c = 'c'; // Type declared - char
    // etc. same for all data types
    printf("a is %d, b is %f, c is %c\n", a, b, c);
    return 0;
}